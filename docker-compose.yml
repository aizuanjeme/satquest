services:
  satquest-backend:
    build: ./backend
    env_file:
      - ./backend/.env
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - breez-data:/data/breez
    networks:
      - satquest-network

networks:
  satquest-network:
    driver: bridge

volumes:
  breez-data:
